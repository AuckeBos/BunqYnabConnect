version: '3'
services:
  bunq_ynab_server:
    container_name: bunqynab
    build: .
    volumes:
      - config:/config
    ports:
      - "10000-10001:10000-10001"
      - "9888:<PORT>"
    restart: always
volumes:
  config: